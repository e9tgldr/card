[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — React Router owns client-side routes
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
